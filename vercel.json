{
  "version": 2,
  "builds": [
    {
      "src": "./api/app.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "./api/definitions",
      "dest": "./api/app.js",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],
      "headers": {
        "Access-Control-Allow-Origin": "*"
      }
    }
  ]
}